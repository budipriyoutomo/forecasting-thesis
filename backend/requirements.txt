# Versi ditulis sebagai lower bound (`>=`), bukan pin keras (`==`), supaya
# instalasi memakai wheel prebuilt di berbagai versi Python (3.11 di CI, 3.14 di
# mesin dev). Pin versi 2024 memaksa build dari source di Python baru — lama dan
# rawan gagal. Untuk reproducibility ketat, buat requirements.lock via pip freeze.

# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.9

# Config & validation
pydantic[email]>=2.9.2
pydantic-settings>=2.5.2

# Auth
pyjwt>=2.9.0

# Data handling
pandas>=2.2.2

# Database (Fase 1+, sudah didaftarkan agar siap dipakai)
sqlalchemy[asyncio]>=2.0.35
alembic>=1.13.2
asyncpg>=0.30.0

# Object storage (Fase 3+)
boto3>=1.35.24        # kompatibel dengan Cloudflare R2 (S3 API)

# Forecasting engines (Fase 4)
statsmodels>=0.14.6
lightgbm>=4.7.0
scikit-learn>=1.5.2
numpy>=1.26

# prophet>=1.1.6   # TODO: belum diaktifkan — butuh build cmdstan yang berat, lihat engines/prophet_engine.py

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx>=0.27.2
