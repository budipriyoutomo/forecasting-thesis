# Versi ditulis sebagai lower bound (`>=`), bukan pin keras (`==`), supaya
# instalasi memakai wheel prebuilt di berbagai versi Python (3.11 di CI, 3.14 di
# mesin dev). Pin versi 2024 memaksa build dari source di Python baru — lama dan
# rawan gagal. Untuk reproducibility ketat, buat requirements.lock via pip freeze.

# Core web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.6
python-multipart>=0.0.9

# Config & validation
pydantic[email]>=2.9.2
pydantic-settings>=2.5.2

# Auth
pyjwt>=2.9.0

# Data handling
pandas>=2.2.2

# Database (Fase 1+, sudah didaftarkan agar siap dipakai)
sqlalchemy[asyncio]>=2.0.35
alembic>=1.13.2
asyncpg>=0.30.0

# Object storage (Fase 3+)
boto3>=1.35.24        # kompatibel dengan Cloudflare R2 (S3 API)

# Export (Fase 8)
openpyxl>=3.1.5       # Excel (.xlsx)
fpdf2>=2.8.0          # PDF (pure-python, tanpa dependency native berat)

# Forecasting engines v3.0 (Fase 4) — Comparative Selection
scikit-learn>=1.5.2   # RandomForestRegressor
xgboost>=2.1.0        # XGBRegressor
numpy>=1.26
# tensorflow>=2.16    # LSTM — TIDAK ada wheel utk Python 3.14 (env dev). Aktifkan di
#                       runtime yg didukung TF (≤3.13); lstm_engine.py import lazy →
#                       tanpa TF, engine ini auto-excluded di mode otomatis (docs §6.4).

# Engine legacy v2.0 (nonaktif default, docs §6.9) — dipertahankan utk reuse raw material
statsmodels>=0.14.6   # ETS/ARIMA legacy
lightgbm>=4.7.0       # LightGBM legacy
# prophet>=1.1.6      # TODO legacy — butuh build cmdstan berat, lihat engines/legacy/prophet_engine.py

# Testing
pytest>=8.3.3
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx>=0.27.2
